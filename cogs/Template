import os
import discord

from discord.ext import commands
from datetime import datetime

class Name(commands.Cog):  
    def __init__(self, bot):
        self.bot = bot
    
    def command(self, ctx): #commands
        pass

def setup(bot):
    bot.add_cog(Name(bot))